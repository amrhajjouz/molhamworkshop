function overView{pascal}ControllerInit ($apiRequest, $page) {
    return $apiRequest.config('{routeName}/' + $page.routeParams.id).getData();
}

function overview{pascal}Controller ($scope, $init) {
    $scope.{singular} = $init;
}
